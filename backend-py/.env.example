# ============================================================
#  RDWeb-Moderno — Configuración del servidor (Python)
#  Copia este archivo como .env y ajusta los valores
# ============================================================

# --- Servidor Uvicorn ---
PORT=3000
NODE_ENV=production

# --- JWT ---
JWT_SECRET=cambia_esto_por_un_secreto_muy_largo_y_aleatorio_en_produccion
JWT_EXPIRES_IN=1h

# --- Active Directory / LDAP ---
LDAP_URL=ldap://SRV-DC.LAB-MH.LOCAL
LDAP_BASE_DN=DC=LAB-MH,DC=LOCAL
AD_DOMAIN=LAB-MH

AD_SERVICE_USER=svc-rdweb@lab-mh.local
AD_SERVICE_PASS=1234Aa*

# --- RD Connection Broker ---
RDCB_SERVER=SRV-APPS.LAB-MH.LOCAL

# --- Modo Simulación (true = datos ficticios, sin AD ni RDCB real) ---
# SIMULATION_MODE=true
